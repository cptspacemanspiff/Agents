Takes a natural-language brief pointing at a root directory and a goal, decomposes the work into bounded worker tasks, and delegates to the component-mapper, component-reviewer, architecture-reviewer, doc-writer, and ducky agents in parallel. Synthesizes their output into a single Markdown report. The orchestrator does the minimum reading needed to plan; workers do the real work. Goal-agnostic — it does not force a specific pipeline.
